function HC = histCost_a2(SCx, SCy)
% SCx, SCy: 28x28x50
% Chi^2-Test zum Vergleich zweier Histogramme
    
    % TODO
    [m, n] = size(SCx);
    HC = 0;
    for i = 1:m
        for j = 1:n
            if (SCx(i, j) + SCy(i, j)) ~= 0
                HC = HC + 0.5*(SCx(i, j) - SCy(i, j))^2/(SCx(i, j) + SCy(i, j));
            end
        end
    end
    
    function meanChi = mean_chi(SCx, SCy)
        Chi = 
    end
end
end
